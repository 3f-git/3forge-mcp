# Local endpoint / per-user overrides — never commit
.env
*.local.json
.mcp.local.json
node_modules/
.DS_Store

# Built-on-demand portable release asset (from build/pack-portable.mjs)
3forge-mcp-portable-*.zip
